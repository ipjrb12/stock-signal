# Workstream E: the dashboard. Python 3.11 or 3.12.
# A superset of nothing else: these six are all the app needs at runtime, because
# it reads the pre-built data pack in data/ rather than fitting anything.

streamlit>=1.49
plotly>=5.22
pandas>=2.2
numpy>=1.26
pyarrow>=15
scipy>=1.11          # policy_engine's optional "normal" aggregation path

# Not needed to run the app. scikit-learn==1.8.0 is required only if you rebuild
# the forecasts from workstream B's saved model bundles.
